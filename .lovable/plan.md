

## خطة: حذف ملف Properties.tsx غير المستخدم

### السبب
- ملف `src/pages/Properties.tsx` غير مستورد في أي مكان بالمشروع
- التوجيه `/properties` يشير إلى `PropertiesManagement.tsx`
- الملف كود ميت يجب إزالته لتنظيف المشروع

### التنفيذ
1. **حذف** ملف `src/pages/Properties.tsx`

تغيير بسيط — ملف واحد فقط للحذف.

