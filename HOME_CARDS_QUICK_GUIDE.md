# إدارة بطاقات الصفحة الرئيسية - الدليل السريع
## Quick Guide for Home Cards Management

---

## ✅ الميزة الجديدة

تم إضافة نظام متكامل لإدارة بطاقات التنقل في الصفحة الرئيسية من لوحة التحكم.

**ما يمكن للمشرفين القيام به:**
- 🔄 إعادة ترتيب البطاقات بسهولة
- ✏️ تعديل العناوين والأوصاف
- 🎨 تغيير الألوان والأيقونات
- 🔗 تحديد المسارات
- 🔐 تحديد متطلبات تسجيل الدخول

---

## 📋 الخطوات السريعة للتطبيق

### 1. تطبيق قاعدة البيانات

```bash
# في Supabase Dashboard > SQL Editor
# نفّذ محتوى هذا الملف:
CREATE_HOME_CARDS_TABLE.sql
```

### 2. تأكد من عدم وجود أخطاء

```bash
npm run build
npm run lint
```

### 3. ابدأ الاختبار

```bash
npm run dev
```

---

## 🎯 كيفية الاستخدام

### 1. الدخول إلى لوحة التحكم
- افتح المتصفح: `http://localhost:8080`
- سجل الدخول كمشرف
- اذهب إلى `/dashboard/home-cards`

### 2. إعادة ترتيب البطاقات
- استخدم أزرار ↑ و ↓ لتحريك البطاقات
- اضغط على زر "حفظ الترتيب" في الأعلى
- ✅ التغييرات تحفظ فوراً

### 3. تعديل محتوى البطاقة
- اضغط زر التعديل (✏️) على البطاقة
- عدّل النصوص والألوان والأيقونة
- اضغط "حفظ التغييرات"
- ✅ التحديثات تظهر فوراً في الصفحة الرئيسية

---

## 📁 الملفات المهمة

### قاعدة البيانات:
```
CREATE_HOME_CARDS_TABLE.sql          # جدول ودوال SQL
```

### الكود:
```
src/hooks/useHomeCards.tsx                        # Hook للبيانات
src/components/Dashboard/HomeCardsManagement.tsx  # واجهة الإدارة
src/pages/Home.tsx                                # الصفحة الرئيسية (محدثة)
src/pages/Dashboard.tsx                           # لوحة التحكم (محدثة)
src/components/Dashboard/DashboardTabs.tsx        # التبويبات (محدثة)
src/hooks/useDashboardNav.ts                      # التنقل (محدث)
src/integrations/supabase/types.ts                # الأنواع (محدثة)
```

### التوثيق:
```
HOME_CARDS_SYSTEM_DOCUMENTATION.md   # التوثيق الكامل
APPLY_HOME_CARDS_SYSTEM.sql          # دليل التطبيق
HOME_CARDS_QUICK_GUIDE.md            # هذا الملف
```

---

## 🔍 التحقق من النجاح

### 1. في قاعدة البيانات
```sql
-- يجب أن تحصل على 4 صفوف
SELECT * FROM home_cards ORDER BY display_order;
```

### 2. في لوحة التحكم
- ✅ يظهر تبويب "بطاقات الرئيسية"
- ✅ تظهر 4 بطاقات قابلة للترتيب والتعديل

### 3. في الصفحة الرئيسية
- ✅ البطاقات تُحمّل من قاعدة البيانات
- ✅ التعديلات تظهر فوراً
- ✅ الترتيب يتطابق مع لوحة التحكم

---

## 🎨 الألوان المتاحة

| اللون | الخلفية | الأيقونة |
|-------|---------|---------|
| أزرق | `bg-blue-50` | `bg-blue-500` |
| بنفسجي | `bg-purple-50` | `bg-purple-500` |
| أخضر | `bg-green-50` | `bg-green-500` |
| برتقالي | `bg-orange-50` | `bg-orange-500` |
| أحمر | `bg-red-50` | `bg-red-500` |
| أصفر | `bg-yellow-50` | `bg-yellow-500` |
| وردي | `bg-pink-50` | `bg-pink-500` |
| نيلي | `bg-indigo-50` | `bg-indigo-500` |

---

## 🎭 الأيقونات المتاحة

- `Search` - بحث 🔍
- `Building2` - مبنى 🏢
- `PlusCircle` - إضافة ➕
- `User` - مستخدم 👤
- `Home` - منزل 🏠
- `MapPin` - موقع 📍

---

## 🐛 حل المشاكل الشائعة

### البطاقات لا تظهر في لوحة التحكم
```bash
# تأكد من تطبيق SQL بنجاح
# تحقق من Supabase SQL Editor
SELECT COUNT(*) FROM home_cards;
# يجب أن يكون 4
```

### التعديلات لا تظهر في الصفحة الرئيسية
```bash
# جرب Hard Refresh
Ctrl + Shift + R

# أو امسح الـ Cache
# أدوات المطور > Application > Clear Storage
```

### أخطاء TypeScript
```bash
# تأكد من تحديث types.ts
# ابحث عن get_active_home_cards في الملف
grep "get_active_home_cards" src/integrations/supabase/types.ts
```

---

## 📊 البيانات الافتراضية

البطاقات الأربعة الموجودة حالياً:

1. **وين اسكن** → `/map` (أزرق)
2. **العقارات المتوفرة الآن** → `/properties` (بنفسجي)
3. **إضافة عقار جديد** → `/add-property` (أخضر، يتطلب دخول ✅)
4. **ملفي الشخصي** → `/profile` (برتقالي، يتطلب دخول ✅)

---

## 🚀 ما التالي؟

### توسعات مستقبلية محتملة:
- [ ] إضافة بطاقات جديدة (وليس تعديل فقط)
- [ ] Drag & Drop للترتيب بدلاً من الأزرار
- [ ] معاينة مباشرة للتعديلات
- [ ] حفظ تاريخ التعديلات
- [ ] إخفاء/إظهار البطاقات بدون حذفها

---

## 📝 الخلاصة

| الجزء | الحالة | الملاحظات |
|------|--------|----------|
| SQL Schema | ✅ جاهز | `CREATE_HOME_CARDS_TABLE.sql` |
| React Hook | ✅ جاهز | `useHomeCards.tsx` |
| Admin UI | ✅ جاهز | `HomeCardsManagement.tsx` |
| Home Page | ✅ جاهز | تحديث `Home.tsx` |
| Dashboard | ✅ جاهز | إضافة التبويب |
| Types | ✅ جاهز | تحديث `types.ts` |
| Documentation | ✅ جاهز | ملف شامل |
| Testing | ⏳ بانتظارك | اختبر بنفسك! |

---

## 💡 نصيحة أخيرة

قبل البدء بالاختبار:

```bash
# 1. طبّق SQL في Supabase
# 2. تأكد من عدم وجود أخطاء
npm run build

# 3. ابدأ الخادم
npm run dev

# 4. افتح المتصفح
http://localhost:8080/dashboard/home-cards
```

---

**أي أسئلة؟**  
راجع `HOME_CARDS_SYSTEM_DOCUMENTATION.md` للتوثيق الكامل.

**تاريخ الإصدار:** 2025-01-XX  
**الحالة:** ✅ Ready for Testing  
**النسخة:** 1.0.0
