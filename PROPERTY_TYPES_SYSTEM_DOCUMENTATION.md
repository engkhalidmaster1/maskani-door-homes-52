## 🚀 ملخص سريع (TL;DR)

هذا المستند يشرح النظام الجديد لأنواع العقارات.

- **ماذا:** تم توسيع أنواع العقارات من 3 إلى 22 نوعًا، مع إضافة 24 حقلاً جديدًا.
- **لماذا:** لتوفير تصنيف أكثر دقة وتفصيلاً للعقارات وتحسين تجربة المستخدم.
- **كيف:** عبر تحديثات في قاعدة البيانات (Enum, حقول جديدة, جدول إعدادات)، تعريفات TypeScript، React Hooks لإدارة الحقول الديناميكية، ومكونات UI تفاعلية.
- **الحالة الحالية:** النظام جاهز للتطبيق، لكن يتطلب **نشر التحديثات على قاعدة البيانات (Migration)** وتحديث واجهات عرض العقار.

---

## 📋 نظرة عامة

تم تطوير نظام شامل لإدارة أنواع العقارات المختلفة مع ربط منطقي بين نوع العقار والحقول المطلوبة/الاختيارية. النظام يدعم **22 نوع عقار** موزعة على **4 فئات رئيسية**.

## 🎯 الأهداف المحققة

1. ✅ توسيع أنواع العقارات من 3 إلى 22 نوع
2. ✅ إضافة 24 حقل جديد للعقارات
3. ✅ نظام تحقق ديناميكي من الحقول المطلوبة
4. ✅ واجهة مستخدم تفاعلية تُظهر الحقول المناسبة فقط
5. ✅ تكامل كامل مع قاعدة البيانات

---

## 📂 البنية الهيكلية

### 1. قاعدة البيانات

**الملف:** `supabase/migrations/20251115120000_property_types_comprehensive_system.sql`

#### التعديلات الرئيسية:

**أ. توسيع Enum:**
```sql
ALTER TYPE property_type ADD VALUE IF NOT EXISTS 'villa';
ALTER TYPE property_type ADD VALUE IF NOT EXISTS 'townhouse';
-- ... (20 نوع إضافي)
```

**ب. الحقول الجديدة (24 حقل):**
- **غرف:** `bathrooms`, `rooms_count`, `floors_count`
- **مساحات:** `land_area`, `building_area`, `street_width`
- **مميزات:** `balcony`, `parking`, `elevator`, `garden`, `garage`, `swimming_pool`, `maid_room`, `driver_room`
- **تجاري:** `street_facing`, `corner`, `licensed`, `cold_storage`, `loading_dock`, `height`, `shop_type`, `office_number`
- **خاص:** `land_type`, `room_type`, `shared_bathroom`

**ج. جدول التكوين:**
```sql
CREATE TABLE property_type_fields_config (
  property_type TEXT PRIMARY KEY,
  category TEXT NOT NULL,
  required_fields TEXT[] NOT NULL,
  optional_fields TEXT[] NOT NULL,
  excluded_fields TEXT[] NOT NULL,
  validation_rules JSONB,
  display_order INTEGER
);
```

**د. دوال التحقق:**
- `property_validation_core_v2()`: التحقق الديناميكي من الحقول
- `get_property_type_config()`: جلب إعدادات نوع العقار
- `list_property_types()`: قائمة جميع الأنواع
- `validate_property_payload()`: التحقق من البيانات قبل الحفظ

---

### 2. أنواع TypeScript

**الملف:** `src/types/propertyTypes.ts`

#### الأنواع المعرفة:

```typescript
// 22 نوع عقار
export type PropertyType = 
  | 'apartment' | 'villa' | 'townhouse' | 'duplex' 
  | 'studio' | 'penthouse' | 'chalet' | 'house'
  | 'shop' | 'showroom' | 'office' | 'warehouse'
  | 'restaurant' | 'cafe' | 'mall' | 'commercial_building'
  | 'residential_land' | 'commercial_land' | 'agricultural_land' | 'farm'
  | 'room' | 'rest_house';

// الفئات
export type PropertyCategory = 
  | 'residential' 
  | 'commercial' 
  | 'land' 
  | 'special';

// نوع العرض
export type ListingType = 'sale' | 'rent';
```

#### الثوابت:

```typescript
// التسميات بالعربية
export const PROPERTY_TYPE_LABELS_AR: Record<PropertyType, string> = {
  apartment: 'شقة',
  villa: 'فيلا',
  townhouse: 'تاون هاوس',
  // ... (22 تسمية)
};

// التصنيفات
export const PROPERTY_CATEGORIES: Record<PropertyType, PropertyCategory> = {
  apartment: 'residential',
  villa: 'residential',
  // ... (22 تصنيف)
};
```

---

### 3. React Hooks

#### أ. `usePropertyTypeFields`

**الوظيفة:** إدارة ظهور الحقول حسب نوع العقار

```typescript
const { 
  fieldVisibility,      // حالة كل حقل (visible, required)
  isFieldVisible,       // دالة للتحقق من الظهور
  isFieldRequired,      // دالة للتحقق من الإلزامية
  isLoading            // حالة التحميل
} = usePropertyTypeFields(propertyType, listingType);
```

**المميزات:**
- جلب التكوين من قاعدة البيانات
- منطق خاص (مثل: التأثيث إلزامي للإيجار السكني)
- تحديث تلقائي عند تغيير النوع

#### ب. `usePropertyValidation`

**الوظيفة:** التحقق من صحة البيانات

```typescript
const { 
  validatePropertyComplete,  // تحقق كامل (أساسي + متقدم)
  validateBasicFields,       // تحقق أساسي فقط
  validateProperty,          // تحقق متقدم فقط
  isValidating              // حالة التحقق
} = usePropertyValidation();
```

**مستويات التحقق:**
1. **أساسي:** الحقول الإلزامية للجميع (السعر، الموقع، الوصف)
2. **متقدم:** الحقول المحددة لكل نوع عقار (من قاعدة البيانات)

---

### 4. المكونات (Components)

#### أ. `PropertyTypeSection`

**الوظيفة:** اختيار نوع العقار بتصميم تفاعلي

**المميزات:**
- عرض 22 نوع مجمعة حسب الفئة (accordion)
- أيقونات وألوان مخصصة لكل فئة
- تجربة مستخدم محسنة (animations)

```tsx
<PropertyTypeSection
  formData={formData}
  onChange={handleFormChange}
  onTypeChange={handleTypeChange}
/>
```

#### ب. `ConditionalPropertyFields`

**الوظيفة:** عرض الحقول الإضافية حسب النوع المختار

**الأقسام:**
1. **تفاصيل الغرف:** غرف النوم، الحمامات، الطوابق
2. **المساحات والأبعاد:** مساحة الأرض، البناء، عرض الشارع
3. **المميزات والمرافق:** شرفة، موقف، مصعد، حديقة، مسبح
4. **تفاصيل تجارية:** نوع المحل، رقم المكتب، الارتفاع
5. **تفاصيل إضافية:** نوع الأرض، نوع الغرفة

```tsx
<ConditionalPropertyFields
  formData={formData}
  fieldVisibility={fieldVisibility}
  onChange={handleFormChange}
/>
```

---

## 🔄 التدفق الوظيفي

### 1. إضافة عقار جديد

```
1. اختيار نوع العقار
   ↓
2. جلب تكوين الحقول من قاعدة البيانات
   ↓
3. عرض الحقول المناسبة فقط
   ↓
4. ملء البيانات
   ↓
5. التحقق (client-side)
   ↓
6. التحقق (server-side via RPC)
   ↓
7. رفع الصور
   ↓
8. حفظ في قاعدة البيانات
   ↓
9. تسجيل الحدث (audit log)
```

### 2. عرض عقار

```
1. جلب بيانات العقار
   ↓
2. تحديد نوع العقار
   ↓
3. جلب تكوين الحقول
   ↓
4. عرض الحقول المتوفرة فقط
```

---

## 📊 أمثلة التكوين

### شقة (Apartment)

**الحقول المطلوبة:**
- `building`, `apartment`, `floor`
- `bedrooms`, `bathrooms`
- `area`, `price`

**الحقول الاختيارية:**
- `furnished`, `balcony`, `parking`, `elevator`

**الحقول المستبعدة:**
- `land_area`, `floors_count`, `shop_type`, `land_type`

### أرض سكنية (Residential Land)

**الحقول المطلوبة:**
- `land_area`, `street_width`
- `land_type`, `price`

**الحقول الاختيارية:**
- `building_area`, `street_facing`, `corner`

**الحقول المستبعدة:**
- `bedrooms`, `bathrooms`, `apartment`, `floor`, `furnished`

### محل تجاري (Shop)

**الحقول المطلوبة:**
- `building`, `floor`
- `area`, `price`
- `shop_type`, `licensed`

**الحقول الاختيارية:**
- `street_facing`, `corner`, `cold_storage`, `parking`

**الحقول المستبعدة:**
- `bedrooms`, `bathrooms`, `furnished`, `land_area`, `land_type`

---

## 🚀 الخطوات التالية (Action Items)

لإتمام تفعيل النظام، يجب تنفيذ الخطوات التالية بالترتيب:

### ✅ الخطوة 1: نشر تحديثات قاعدة البيانات (Migration)

هذه هي أهم خطوة. بدونها، لن يعمل النظام.

**الطريقة الموصى بها (باستخدام Supabase CLI):**
```bash
# 1. تأكد من تسجيل الدخول
supabase login

# 2. ربط المشروع (إذا لم يكن مربوطاً)
supabase link --project-ref <YOUR-PROJECT-ID>

# 3. نشر آخر التحديثات
supabase db push
```

**الطريقة اليدوية (في حال وجود مشاكل):**
1. اذهب إلى `SQL Editor` في لوحة تحكم Supabase.
2. انسخ **كامل** محتوى الملف: `supabase/migrations/20251115120000_property_types_comprehensive_system.sql`.
3. الصق المحتوى في المحرر وقم بتشغيله.

> **⚠️ تحذير:** لا تقم بتنفيذ الخطوة التالية قبل نجاح هذه الخطوة.

### ✅ الخطوة 2: تحديث واجهات العرض

يجب تحديث المكونات التي تعرض تفاصيل العقار لتشمل الحقول الجديدة.

**الملفات الرئيسية للتعديل:**
- `src/components/PropertyCard.tsx`: لإظهار معلومات أساسية جديدة (مثل عدد الغرف).
- `src/components/PropertyDetails.tsx`: لعرض جميع التفاصيل الجديدة بشكل منظم.
- `src/pages/PropertyView.tsx`: الصفحة الرئيسية لعرض العقار.

**المطلوب:**
- استخدام `usePropertyTypeFields` لتحديد الحقول التي يجب عرضها لكل نوع عقار.
- تصميم أقسام جديدة لعرض المميزات (Amenities) والتفاصيل التجارية والأراضي.

### ✅ الخطوة 3: إجراء اختبار شامل

بعد تنفيذ الخطوتين السابقتين، يجب اختبار النظام بالكامل للتأكد من أن كل شيء يعمل كما هو متوقع.

**قائمة التحقق للاختبار:**
- [ ] **إضافة عقار:** قم بإضافة عقار من كل فئة رئيسية (سكني، تجاري، أراضي، خاص) وتأكد من:
    - ظهور الحقول الصحيحة فقط.
    - عمل التحقق من الحقول الإلزامية.
    - حفظ البيانات بنجاح في قاعدة البيانات.
- [ ] **عرض عقار:** تحقق من أن صفحة عرض العقار تعرض البيانات الجديدة بشكل صحيح.
- [ ] **تعديل عقار:** قم بتعديل عقار وتغيير نوعه. تأكد من أن الحقول تتغير ديناميكيًا.
- [ ] **البيانات القديمة:** تأكد من أن العقارات القديمة (التي أُضيفت قبل هذا النظام) لا تزال تُعرض بشكل صحيح.

---

## 📝 ملاحظات هامة

> **🔒 الأمان (Security)**
> - جميع استدعاءات الدوال (RPCs) محمية بسياسات RLS.
> - يتم التحقق من صلاحيات المستخدم من جهة الخادم (Server-side validation).
> - يتم تسجيل جميع الإجراءات الهامة في سجلات التدقيق (Audit Logs).

> **⚡ الأداء (Performance)**
> - استخدام تقنيات `Memoization` للمكونات لتقليل إعادة التصيير (re-rendering).
> - تحميل الحقول بشكل كسول (Lazy loading) لتحسين سرعة التحميل الأولية.
> - تخزين مؤقت (Caching) لإعدادات أنواع العقارات لتقليل طلبات قاعدة البيانات.

> **🔄 التوافقية (Compatibility)**
> - النظام متوافق مع البيانات القديمة (الـ 3 أنواع الأساسية).
> - عملية الـ Migration مصممة لتكون آمنة ولا تسبب فقدان للبيانات.
> - يوجد نظام احتياطي (Fallback) للتعامل مع الحقول غير الموجودة في البيانات القديمة.

> **🔧 الصيانة (Maintainability)**
> - الكود منظم وموثق بشكل جيد.
> - استخدام TypeScript يضمن سلامة الأنواع (Type Safety).
> - لا توجد أخطاء Linting (0 Linter errors).
> - الـ Hooks والمكونات مصممة لتكون قابلة لإعادة الاستخدام.

---

## 🎨 التحسينات المستقبلية

1. **بحث متقدم:** فلترة حسب الحقول الجديدة
2. **إحصائيات:** تقارير مفصلة حسب نوع العقار
3. **مقارنة:** مقارنة عقارات من نفس النوع
4. **AI Recommendations:** اقتراحات ذكية حسب التفضيلات
5. **تصدير:** تصدير البيانات بصيغ مختلفة

---

## 📞 الدعم

للأسئلة أو المشاكل:
- مراجعة `PROPERTY_TYPES_LOGIC_PLAN.md` للتفاصيل الكاملة
- فحص `console logs` للأخطاء
- التحقق من Supabase Dashboard للـ RPCs

---

**تاريخ التحديث:** 15 نوفمبر 2025  
**الإصدار:** 2.0.0  
**الحالة:** جاهز للنشر (pending migration deployment)