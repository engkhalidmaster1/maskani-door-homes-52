# 🚀 دليل التطوير السريع - تطبيق مسكني

> **آخر تحديث**: 20 نوفمبر 2025

---

## 📌 نظرة عامة سريعة

**مسكني** هو تطبيق إدارة عقارات متكامل يعمل كتطبيق ويب وسطح مكتب، مبني بتقنيات حديثة ويدعم اللغة العربية بالكامل مع دعم RTL.

### المعلومات الأساسية
- **النوع**: تطبيق ويب + تطبيق سطح مكتب (Electron)
- **الإصدار**: 1.0.0
- **اللغة الأساسية**: العربية (RTL)
- **السوق المستهدف**: السوق العقاري السعودي

---

## 🎯 الميزات الرئيسية المنجزة

### ✅ الميزات الأساسية
- [x] نظام المصادقة والأمان (تسجيل دخول/تسجيل)
- [x] إدارة العقارات الكاملة (إضافة/تعديل/حذف/نشر)
- [x] نظام رفع الصور مع المعاينة
- [x] نظام المفضلة
- [x] لوحة تحكم المدير
- [x] البحث والفلترة المتقدمة
- [x] إدارة الملف الشخصي
- [x] نظام الأدوار (مستخدم/ناشر/مالك موثوق/وكيل/مدير)

### ✅ الميزات المتقدمة
- [x] العمل بدون اتصال (Offline Support)
- [x] المزامنة الذكية
- [x] PWA (Progressive Web App)
- [x] تطبيق سطح مكتب (Electron)
- [x] خرائط تفاعلية (Leaflet)
- [x] نظام GPS للمواقع
- [x] سجلات التدقيق (Audit Logs)
- [x] Google Analytics Integration

---

## 🛠️ التقنيات المستخدمة

### Frontend Stack
```
React 18 + TypeScript
├── Vite (Build Tool)
├── React Router (Navigation)
├── TanStack Query (State Management)
└── React Hook Form (Forms)
```

### UI/UX Stack
```
Tailwind CSS + shadcn/ui
├── Radix UI (Headless Components)
├── Lucide React (Icons)
├── Sonner (Toast Notifications)
└── Recharts (Charts)
```

### Backend Stack
```
Supabase (BaaS)
├── PostgreSQL (Database)
├── Supabase Auth (Authentication)
├── Supabase Storage (File Storage)
└── Row Level Security (RLS)
```

### Desktop Stack
```
Electron
├── electron-store (Settings)
├── IPC (Inter-Process Communication)
└── electron-builder (Packaging)
```

### Additional Tools
```
├── Leaflet (Maps)
├── Fuse.js (Search)
├── date-fns (Date Handling)
├── Zod (Validation)
├── Vitest (Testing)
└── ESLint (Linting)
```

---

## 📁 بنية المشروع

```
maskani 1.0.0.0/
│
├── src/                          # الكود المصدري
│   ├── main.tsx                  # نقطة الدخول
│   ├── App.tsx                   # الراوتر الرئيسي
│   ├── index.css                 # الأنماط العامة
│   │
│   ├── components/               # المكونات القابلة لإعادة الاستخدام
│   │   ├── Layout/               # مكونات التخطيط
│   │   │   ├── AppLayout.tsx
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── Footer.tsx
│   │   ├── ui/                   # مكونات shadcn/ui
│   │   └── Property/             # مكونات العقارات
│   │
│   ├── pages/                    # صفحات التطبيق
│   │   ├── Home.tsx              # الصفحة الرئيسية
│   │   ├── AddProperty.tsx       # إضافة عقار
│   │   ├── PropertiesManagement.tsx
│   │   ├── Dashboard.tsx         # لوحة التحكم
│   │   ├── MapPage.tsx           # الخرائط
│   │   ├── Profile.tsx           # الملف الشخصي
│   │   ├── Favorites.tsx         # المفضلة
│   │   ├── Login.tsx
│   │   └── Register.tsx
│   │
│   ├── hooks/                    # Custom Hooks
│   │   ├── useAuth.tsx           # المصادقة
│   │   ├── useProperties.tsx     # العقارات
│   │   ├── useFavorites.tsx      # المفضلة
│   │   ├── useOfflineSync.tsx    # المزامنة
│   │   ├── useUserStatus.tsx     # حالة المستخدم
│   │   └── useOptimizedImageUpload.tsx
│   │
│   ├── integrations/             # التكاملات الخارجية
│   │   └── supabase/
│   │       ├── client.ts         # عميل Supabase
│   │       └── types.ts          # أنواع قاعدة البيانات
│   │
│   ├── services/                 # الخدمات
│   │   └── RBACManager.ts        # إدارة الصلاحيات
│   │
│   ├── constants/                # الثوابت
│   │   ├── markets.ts            # أسواق الرياض
│   │   └── propertyTypes.ts      # أنواع العقارات
│   │
│   ├── context/                  # React Context
│   │   └── SettingsContext.tsx
│   │
│   ├── lib/                      # المساعدات
│   │   ├── utils.ts
│   │   └── formatters.ts
│   │
│   └── types/                    # TypeScript Types
│
├── public/                       # الملفات الثابتة
│   ├── electron.cjs              # Electron Main Process
│   ├── preload.cjs               # Electron Preload
│   ├── sw.js                     # Service Worker
│   ├── manifest.json             # PWA Manifest
│   └── icons/                    # الأيقونات
│
├── supabase/                     # إعدادات Supabase
│   ├── migrations/               # ملفات الهجرة
│   └── functions/                # Edge Functions
│
├── scripts/                      # سكريبتات مساعدة
│
├── dist/                         # مخرجات البناء (Web)
├── dist-electron/                # مخرجات البناء (Desktop)
│
├── package.json                  # التبعيات والسكريبتات
├── vite.config.ts                # إعدادات Vite
├── tailwind.config.ts            # إعدادات Tailwind
├── tsconfig.json                 # إعدادات TypeScript
├── .env.local                    # المتغيرات البيئية
│
└── [وثائق متعددة].md            # ملفات التوثيق
```

---

## 🚀 البدء السريع

### 1. المتطلبات الأساسية
```bash
Node.js 18+
npm أو bun
حساب Supabase
```

### 2. التثبيت
```bash
# استنساخ المشروع
cd "d:\projects\sakani\maskani 1.0.0.0"

# تثبيت التبعيات
npm install
```

### 3. إعداد البيئة
```bash
# نسخ ملف البيئة
cp .env.example .env.local

# تعديل المتغيرات في .env.local
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_GOOGLE_ANALYTICS_ID=your_ga_id
```

### 4. التشغيل

#### تطبيق الويب
```bash
# التطوير (المنفذ 8081)
npm run dev

# البناء للإنتاج
npm run build

# معاينة البناء
npm run preview
```

#### تطبيق سطح المكتب
```bash
# التطوير
npm run electron-dev

# البناء والتغليف
npm run dist

# بناء لجميع المنصات
npm run dist-all
```

---

## 🔑 الأوامر المهمة

### التطوير
```bash
npm run dev              # تشغيل خادم التطوير
npm run electron-dev     # تشغيل Electron مع التطوير
npm run type-check       # فحص أنواع TypeScript
npm run lint             # فحص الكود
npm run lint:fix         # إصلاح مشاكل الكود تلقائياً
```

### الاختبار
```bash
npm run test             # تشغيل الاختبارات (watch mode)
npm run test:run         # تشغيل الاختبارات مرة واحدة
npm run test:coverage    # تقرير التغطية
npm run test:ui          # واجهة الاختبارات
```

### البناء والنشر
```bash
npm run build            # بناء تطبيق الويب
npm run build:dev        # بناء للتطوير
npm run dist             # بناء تطبيق سطح المكتب
npm run package-win      # بناء لـ Windows فقط
npm run ci               # فحص شامل قبل النشر
npm run quality-check    # فحص الجودة
```

---

## 🗄️ قاعدة البيانات

### الجداول الرئيسية

#### `profiles`
```sql
- id (uuid)
- email (text)
- full_name (text)
- role (text) -- user, publisher, verified_owner, office_agent, admin
- status (text) -- active, banned, pending
- avatar_url (text)
- phone (text)
- created_at (timestamp)
```

#### `properties`
```sql
- id (uuid)
- user_id (uuid) -- FK to profiles
- title (text)
- description (text)
- property_type (text)
- listing_type (text) -- sale, rent
- price (numeric)
- area (numeric)
- location (text)
- latitude (numeric)
- longitude (numeric)
- bedrooms (integer)
- bathrooms (integer)
- images (jsonb)
- status (text) -- published, hidden, pending
- created_at (timestamp)
```

#### `favorites`
```sql
- id (uuid)
- user_id (uuid) -- FK to profiles
- property_id (uuid) -- FK to properties
- created_at (timestamp)
```

#### `offices`
```sql
- id (uuid)
- name (text)
- license_number (text)
- phone (text)
- email (text)
- location (text)
- created_at (timestamp)
```

### Row Level Security (RLS)
- جميع الجداول محمية بـ RLS
- المستخدمون يمكنهم رؤية وتعديل بياناتهم فقط
- المدراء لديهم صلاحيات كاملة

---

## 🎨 نظام التصميم

### الألوان الرئيسية
```css
--primary: الأزرق الداكن
--secondary: الأخضر
--accent: البرتقالي
--background: الأبيض/الرمادي الفاتح
--foreground: الأسود/الرمادي الداكن
```

### الخطوط
```css
font-family: 'Tajawal', sans-serif;
```

### الأيقونات
```jsx
import { Icon } from 'lucide-react'
```

---

## 🔐 نظام الصلاحيات

### الأدوار
1. **user** - مستخدم عادي (يمكنه التصفح والمفضلة)
2. **publisher** - ناشر (يمكنه إضافة عقارات)
3. **verified_owner** - مالك موثوق (ناشر + شارة التحقق)
4. **office_agent** - وكيل مكتب (ناشر + ربط بمكتب)
5. **admin** - مدير (صلاحيات كاملة)

### الصلاحيات
```typescript
// في RBACManager.ts
canAddProperty()    // publisher+
canEditProperty()   // owner or admin
canDeleteProperty() // owner or admin
canManageUsers()    // admin only
canViewDashboard()  // admin only
```

---

## 🐛 المشاكل المعروفة

### 1. مشاكل ESLint
- **الحالة**: 40 خطأ، 18 تحذير
- **السبب الرئيسي**: استخدام `any` في بعض الملفات
- **الملفات المتأثرة**: 
  - `src/hooks/useAuth.tsx`
  - `src/hooks/useFavorites.tsx`
  - `src/hooks/useUserStatus.tsx`
- **الحل المقترح**: استبدال `any` بأنواع محددة

### 2. مشاكل الأداء
- **Re-renders مفرطة** في PropertyCard و PropertiesList
- **الحل المقترح**: استخدام React.memo و useMemo

### 3. مشاكل التصميم
- **Responsive design** غير مثالي في بعض الصفحات
- **Accessibility** غير كامل
- **الحل المقترح**: مراجعة شاملة للتصميم

---

## 📝 نقاط الضعف في الكود

### من تحليل `project_context.md`:

1. **إدارة الحالة**
   - استخدام useState المفرط
   - عدم فصل منطق الأعمال عن UI
   - نقص في memoization

2. **الأداء**
   - عدم تحسين re-renders
   - تحميل بيانات غير فعال
   - نقص في error boundaries

3. **إدارة الأخطاء**
   - معالجة أخطاء غير شاملة
   - عدم وجود fallback UI
   - logging غير كافي

4. **الاختبار**
   - عدم وجود unit tests كافية
   - عدم وجود integration tests
   - عدم وجود E2E tests

---

## 🎯 خطة التطوير المستقبلية

### المرحلة 1 (2-3 أسابيع) - أولوية عالية
- [ ] نظام الرسائل والتواصل المباشر
- [x] معرض صور محسن
- [ ] نظام المقارنة بين العقارات

### المرحلة 2 (4-6 أسابيع) - أولوية متوسطة
- [ ] البحث المتقدم والفلترة الذكية
- [ ] الخرائط التفاعلية المحسنة
- [ ] نظام الإشعارات الذكية
- [ ] نظام التقييمات والمراجعات

### المرحلة 3 (6-8 أسابيع) - ميزات متقدمة
- [ ] نظام الحجوزات والمواعيد
- [ ] لوحة تحكم متقدمة مع إحصائيات
- [ ] نظام التحقق من الهوية
- [ ] تطبيق PWA متقدم

### المرحلة 4 (8-10 أسابيع) - ميزات ذكية
- [ ] ذكاء اصطناعي لتقدير الأسعار
- [ ] دليل المجمع والمناطق
- [ ] نظام الصيانة والشكاوى

### المرحلة 5 (4-6 أسابيع) - تحسينات
- [ ] تحسينات الأداء
- [ ] تحسينات الأمان
- [ ] تحسينات تجربة المستخدم

---

## 💡 نصائح للتطوير

### 1. قبل البدء بأي ميزة
```bash
# تحديث الكود
git pull

# فحص الجودة
npm run quality-check

# تشغيل التطبيق
npm run dev
```

### 2. أثناء التطوير
- استخدم TypeScript بشكل صحيح (تجنب `any`)
- اتبع مبدأ DRY (Don't Repeat Yourself)
- أضف تعليقات للكود المعقد
- استخدم React.memo للمكونات الثقيلة
- اختبر على أجهزة مختلفة

### 3. قبل الـ Commit
```bash
# فحص الأنواع
npm run type-check

# إصلاح مشاكل الكود
npm run lint:fix

# تشغيل الاختبارات
npm run test:run
```

### 4. البنية المقترحة للمكونات
```typescript
// 1. Imports
import { useState } from 'react'
import { Button } from '@/components/ui/button'

// 2. Types
interface Props {
  title: string
}

// 3. Component
export const MyComponent = ({ title }: Props) => {
  // 4. Hooks
  const [state, setState] = useState()
  
  // 5. Handlers
  const handleClick = () => {}
  
  // 6. Render
  return <div>{title}</div>
}
```

---

## 📚 موارد مفيدة

### الوثائق الرسمية
- [React Docs](https://react.dev)
- [TypeScript Docs](https://www.typescriptlang.org/docs)
- [Vite Docs](https://vitejs.dev)
- [Supabase Docs](https://supabase.com/docs)
- [Tailwind CSS Docs](https://tailwindcss.com/docs)
- [shadcn/ui Docs](https://ui.shadcn.com)

### ملفات المشروع المهمة
- `DEVELOPMENT_PLAN.md` - خطة التطوير الكاملة
- `project_context.md` - سياق المشروع ونقاط الضعف
- `STATUS.md` - حالة التطبيق الحالية
- `README.md` - دليل الاستخدام الأساسي

---

## 🆘 استكشاف الأخطاء

### المشكلة: التطبيق لا يعمل
```bash
# حذف الملفات المؤقتة
rm -rf node_modules dist dist-electron
npm install
npm run dev
```

### المشكلة: أخطاء TypeScript
```bash
npm run type-check
# راجع الأخطاء وأصلحها
```

### المشكلة: Supabase لا يتصل
```bash
# تحقق من .env.local
# تأكد من صحة VITE_SUPABASE_URL و VITE_SUPABASE_ANON_KEY
```

### المشكلة: Electron لا يعمل
```bash
npm run build
npm run electron
# تحقق من console logs
```

---

## ✅ Checklist للمطورين الجدد

- [ ] قرأت هذا الملف بالكامل
- [ ] ثبّت جميع المتطلبات الأساسية
- [ ] أعددت ملف `.env.local`
- [ ] شغّلت `npm install` بنجاح
- [ ] شغّلت `npm run dev` ورأيت التطبيق
- [ ] قرأت `DEVELOPMENT_PLAN.md`
- [ ] قرأت `project_context.md`
- [ ] فهمت بنية المشروع
- [ ] فهمت نظام الصلاحيات
- [ ] جاهز للبدء بالتطوير! 🚀

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع هذا الملف
2. راجع ملفات التوثيق الأخرى
3. تحقق من console logs
4. ابحث في GitHub Issues
5. اسأل الفريق

---

**تم إنشاء هذا الملف في**: 20 نوفمبر 2025  
**الغرض**: تسريع فهم المشروع للمطورين الجدد والحاليين

**حظاً موفقاً في التطوير! 💪**
