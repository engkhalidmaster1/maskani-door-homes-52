# 🧪 دليل الاختبار الشامل - Maskani

> **التاريخ**: 20 نوفمبر 2025  
> **الهدف**: اختبار المكونات الجديدة والتحسينات

---

## 📋 نظرة عامة على الاختبارات

### المكونات المطلوب اختبارها:
1. ✅ PropertyCard.optimized
2. ✅ Advanced Pagination
3. ✅ Skeleton Loaders
4. ✅ Error Boundaries
5. ✅ Performance Hooks (useDebounce, useThrottle)

---

## 🎯 اختبار PropertyCard المحسن

### 1. اختبار وظيفي

#### Test Case 1: عرض البيانات
```
الخطوات:
1. افتح صفحة العقارات
2. تحقق من عرض جميع العقارات

النتيجة المتوقعة:
✅ عرض الصورة بشكل صحيح
✅ عرض العنوان
✅ عرض السعر منسقاً
✅ عرض الموقع
✅ عرض حالة العقار (منشور/مخفي)
```

#### Test Case 2: المفضلة
```
الخطوات:
1. اضغط على أيقونة القلب
2. انتظر التحديث
3. تحقق من تغيير اللون

النتيجة المتوقعة:
✅ تتحول الأيقونة للون الأحمر
✅ يتم حفظ الحالة
✅ لا يحدث re-render للبطاقات الأخرى
```

#### Test Case 3: النشر/الإخفاء
```
الخطوات:
1. اضغط على زر النشر/الإخفاء
2. انتظر التحديث
3. تحقق من تغيير الحالة

النتيجة المتوقعة:
✅ تتغير حالة العقار
✅ تتحدث الأيقونة والنص
✅ لا يحدث re-render للبطاقات الأخرى
```

### 2. اختبار الأداء

#### Performance Test 1: عدد Re-renders
```bash
# استخدم React DevTools Profiler

الخطوات:
1. افتح React DevTools → Profiler
2. ابدأ التسجيل
3. قم بتحديث عقار واحد (مثلاً، أضفه للمفضلة)
4. أوقف التسجيل
5. احسب عدد مرات re-render لكل PropertyCard

النتيجة المتوقعة:
❌ قبل: 50 re-render (جميع البطاقات)
✅ بعد: 1-2 re-render (البطاقة المتغيرة فقط)
```

#### Performance Test 2: وقت التصيير
```bash
الخطوات:
1. افتح React DevTools → Profiler
2. قس وقت التصيير لقائمة 50 عقار
3. قارن قبل وبعد التحسين

النتيجة المتوقعة:
✅ تحسن 20-30% في وقت التصيير
```

---

## 🔢 اختبار نظام Pagination

### Test Case 1: Client-side Pagination

```typescript
// ملف الاختبار: src/__tests__/usePagination.test.ts

test('يجب أن يقسم البيانات بشكل صحيح', () => {
  const data = Array.from({ length: 100 }, (_, i) => ({ id: i }));
  const { result } = renderHook(() => usePagination({ 
    data, 
    initialPageSize: 20 
  }));

  // التحقق
  expect(result.current.totalPages).toBe(5);
  expect(result.current.paginatedData.length).toBe(20);
  expect(result.current.currentPage).toBe(1);
});

test('يجب أن ينتقل للصفحة التالية', () => {
  const data = Array.from({ length: 100 }, (_, i) => ({ id: i }));
  const { result } = renderHook(() => usePagination({ 
    data, 
    initialPageSize: 20 
  }));

  act(() => {
    result.current.nextPage();
  });

  expect(result.current.currentPage).toBe(2);
  expect(result.current.paginatedData[0].id).toBe(20);
});
```

### Test Case 2: UI Testing

```
الخطوات:
1. افتح صفحة العقارات
2. تحقق من ظهور Pagination
3. اضغط على "التالي"
4. تحقق من تغيير البيانات

النتيجة المتوقعة:
✅ تظهر الصفحة الثانية
✅ يتغير رقم الصفحة الحالية
✅ تتحدث البيانات المعروضة
```

### Test Case 3: اختيار حجم الصفحة

```
الخطوات:
1. افتح قائمة حجم الصفحة
2. اختر 50 بدلاً من 20
3. تحقق من التحديث

النتيجة المتوقعة:
✅ يتم عرض 50 عقار
✅ يتم إعادة حساب عدد الصفحات
✅ يتم الرجوع للصفحة الأولى
```

---

## 💀 اختبار Skeleton Loaders

### Test Case 1: PropertyListSkeleton

```
الخطوات:
1. افتح Network tab في DevTools
2. اضبط سرعة الشبكة على "Slow 3G"
3. قم بتحميل صفحة العقارات
4. راقب عرض Skeleton

النتيجة المتوقعة:
✅ يظهر Skeleton فوراً
✅ Animation سلسة (shimmer)
✅ يختفي عند تحميل البيانات
✅ لا يحدث "layout shift"
```

### Test Case 2: TableSkeleton

```
الخطوات:
1. افتح صفحة المستخدمين
2. راقب Skeleton أثناء التحميل

النتيجة المتوقعة:
✅ يظهر هيكل الجدول
✅ عدد الأعمدة صحيح
✅ عدد الصفوف مطابق
```

---

## 🚨 اختبار Error Boundaries

### Test Case 1: PropertyErrorFallback

```typescript
// اختبار يدوي - قم برمي خطأ متعمد

// في PropertyCard.tsx (مؤقتاً للاختبار):
throw new Error("Test error");

الخطوات:
1. تحديث الصفحة
2. راقب Error Fallback

النتيجة المتوقعة:
✅ يظهر UI جميل للخطأ
✅ رسالة واضحة
✅ زر "إعادة المحاولة" يعمل
```

### Test Case 2: NetworkErrorFallback

```
الخطوات:
1. قطع الاتصال بالإنترنت
2. حاول تحميل العقارات
3. راقب Error Message

النتيجة المتوقعة:
✅ يظهر UI خاص بأخطاء الشبكة
✅ رسالة واضحة عن المشكلة
✅ زر "إعادة المحاولة" يعمل
```

---

## ⚡ اختبار Performance Hooks

### Test Case 1: useDebounce

```typescript
// ملف الاختبار: src/__tests__/useDebounce.test.ts

test('يجب أن يؤخر تحديث القيمة', async () => {
  const { result, rerender } = renderHook(
    ({ delay, value }) => useDebounce(value, delay),
    { initialProps: { delay: 500, value: 'initial' } }
  );

  expect(result.current).toBe('initial');

  // تحديث القيمة
  rerender({ delay: 500, value: 'updated' });

  // يجب أن تبقى القيمة كما هي فوراً
  expect(result.current).toBe('initial');

  // بعد 500ms يجب أن تتحدث
  await waitFor(() => {
    expect(result.current).toBe('updated');
  }, { timeout: 600 });
});
```

### Test Case 2: UI Testing للبحث

```
الخطوات:
1. افتح صفحة البحث
2. اكتب في حقل البحث بسرعة
3. راقب طلبات API في Network tab

النتيجة المتوقعة:
✅ لا يتم إرسال طلب مع كل حرف
✅ يتم إرسال طلب واحد بعد التوقف عن الكتابة (500ms)
✅ النتائج تظهر بشكل صحيح
```

---

## 📊 اختبارات التكامل

### Integration Test 1: القائمة الكاملة

```
الخطوات:
1. افتح صفحة العقارات
2. انتظر التحميل (Skeleton)
3. تحقق من عرض البيانات
4. اختبر Pagination
5. اختبر البحث مع Debounce
6. اختبر المفضلة

النتيجة المتوقعة:
✅ جميع المكونات تعمل معاً بسلاسة
✅ لا تعارض بين المكونات
✅ UX سلسة ومتجاوبة
```

### Integration Test 2: حالات الخطأ

```
الخطوات:
1. اختبر مع خطأ شبكة
2. اختبر مع بيانات فارغة
3. اختبر مع بيانات غير صحيحة

النتيجة المتوقعة:
✅ Error Boundaries تعمل
✅ Fallback UI يظهر
✅ إمكانية إعادة المحاولة
```

---

## 🎯 معايير القبول

### الأداء:
```
✅ Re-renders أقل بنسبة 40%+
✅ وقت التصيير أسرع بنسبة 20%+
✅ استهلاك ذاكرة أقل بنسبة 30%+
```

### الوظائف:
```
✅ جميع الميزات تعمل كما السابق
✅ لا أخطاء في Console
✅ لا تحذيرات React
```

### تجربة المستخدم:
```
✅ Skeleton Loaders سلسة
✅ Pagination سريعة
✅ البحث متجاوب
✅ Error Messages واضحة
```

---

## 📝 Checklist الاختبار

### الاختبارات الأساسية:
- [ ] PropertyCard - عرض البيانات
- [ ] PropertyCard - المفضلة
- [ ] PropertyCard - النشر/الإخفاء
- [ ] PropertyCard - الأداء (Re-renders)
- [ ] Pagination - التنقل بين الصفحات
- [ ] Pagination - تغيير حجم الصفحة
- [ ] Skeleton - عرض أثناء التحميل
- [ ] Error Boundary - معالجة الأخطاء
- [ ] Debounce - البحث

### اختبارات التكامل:
- [ ] القائمة الكاملة تعمل
- [ ] لا تعارض بين المكونات
- [ ] الأداء جيد

### اختبارات المتصفحات:
- [ ] Chrome
- [ ] Firefox
- [ ] Safari
- [ ] Edge

### اختبارات الأجهزة:
- [ ] Desktop
- [ ] Tablet
- [ ] Mobile

---

## 🐛 تسجيل المشاكل

إذا وجدت أي مشكلة، سجلها بهذا الشكل:

```markdown
### المشكلة: [وصف مختصر]

**الخطوات لإعادة الإنتاج:**
1. ...
2. ...

**النتيجة الفعلية:**
...

**النتيجة المتوقعة:**
...

**البيئة:**
- المتصفح: ...
- الجهاز: ...
- النسخة: ...

**Screenshots/Videos:**
[إذا توفرت]
```

---

## 🎓 نصائح للاختبار

### 1. ابدأ بالأساسيات
```
✅ اختبر الوظائف الأساسية أولاً
✅ ثم اختبر الحالات الخاصة
✅ أخيراً اختبر الأداء
```

### 2. استخدم الأدوات المناسبة
```
✅ React DevTools للأداء
✅ Network tab للشبكة
✅ Console للأخطاء
```

### 3. سجل كل شيء
```
✅ النتائج
✅ المشاكل
✅ الملاحظات
```

---

**آخر تحديث**: 20 نوفمبر 2025  
**الحالة**: ✅ جاهز للاستخدام  
**المطور**: Antigravity AI

🧪 **اختبار موفق!**
