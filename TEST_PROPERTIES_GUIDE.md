# 📝 دليل استخدام البيانات الاختبار ية للعقارات

> **التاريخ**: 20 نوفمبر 2025  
> **الملف**: `INSERT_TEST_PROPERTIES.sql`  
> **الحالة**: ✅ جاهز للتشغيل

---

## 📋 نظرة عامة

تم إنشاء ملف SQL يحتوي على **20 عقار اختباري** يغطي جميع أنواع العقارات المدعومة في النظام (22 نوع).

### البيانات المضافة:

```
✅ 7 عقارات سكنية (Residential)
✅ 6 عقارات تجارية (Commercial)
✅ 4 أراضي (Land)
✅ 2 عقارات خاصة (Special)
✅ 1 عقار تجاري قديم (Legacy)

المجموع: 20 عقار
```

---

## 🎯 جميع الأنواع المغطاة

### 1. السكنية (7 أنواع)
1. ✅ **شقة** (Apartment) - للبيع
2. ✅ **منزل** (House) - للبيع
3. ✅ **فيلا** (Villa) - للبيع
4. ✅ **استوديو** (Studio) - للإيجار
5. ✅ **دوبلكس** (Duplex) - للبيع
6. ✅ **بنتهاوس** (Penthouse) - للبيع
7. ✅ **غرفة** (Room) - للإيجار

### 2. التجارية (6 أنواع)
8. ✅ **محل تجاري** (Shop) - للإيجار
9. ✅ **مكتب** (Office) - للإيجار
10. ✅ **مستودع** (Warehouse) - للإيجار
11. ✅ **معرض** (Showroom) - للإيجار
12. ✅ **مطعم/كافيه** (Restaurant/Cafe) - للإيجار
13. ✅ **عيادة** (Clinic) - للإيجار

### 3. الأراضي (4 أنواع)
14. ✅ **أرض سكنية** (Residential Land) - للبيع
15. ✅ **أرض تجارية** (Commercial Land) - للبيع
16. ✅ **أرض زراعية** (Agricultural Land) - للبيع
17. ✅ **أرض صناعية** (Industrial Land) - للبيع

### 4. الخاصة (2 نوع)
18. ✅ **مزرعة** (Farm) - للبيع
19. ✅ **شاليه** (Chalet) - للإيجار

### 5. القديم (Legacy)
20. ⚠️ **تجاري (قديم)** (Commercial) - للتوافق مع البيانات القديمة

---

## 🚀 كيفية الاستخدام

### الطريقة 1: عبر Supabase Dashboard (موصى بها)

```bash
1. اذهب إلى Supabase Dashboard
2. افتح SQL Editor
3. افتح ملف INSERT_TEST_PROPERTIES.sql
4. ** مهم جداً **: ابحث عن السطر test_user_id
5. استبدل UUID الافتراضي بـ user_id فعلي من قاعدتك
6. نفذ الـ SQL
```

#### الحصول على User ID:

```sql
-- اختر أحد المستخدمين
SELECT id, email FROM auth.users LIMIT 5;

-- أو احصل على User ID الخاص بك
SELECT id FROM auth.users WHERE email = 'your-email@example.com';
```

### الطريقة 2: عبر Supabase CLI

```bash
# 1. تأكد من تسجيل الدخول
supabase login

# 2. تعديل الملف (غيّر user_id)
# افتح INSERT_TEST_PROPERTIES.sql وحدّث user_id

# 3. نفذ الملف
supabase db execute --file INSERT_TEST_PROPERTIES.sql
```

---

## ⚠️ ملاحظات مهمة جداً

### قبل التنفيذ:

```sql
-- 🔴 تحذير: يجب تغيير هذا السطر في الملف!
test_user_id := 'aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee'; -- ❌ افتراضي

-- ✅ إلى UUID مستخدم فعلي:
test_user_id := '12345678-1234-1234-1234-123456789012'; -- ✅ فعلي
```

### أو دع السكريبت يختار تلقائياً:

السكريبت سيحاول تلقائياً اختيار أول مستخدم:

```sql
SELECT id INTO test_user_id FROM auth.users LIMIT 1;
```

---

## 📊 البيانات المضمنة

### لكل عقار:

#### الحقول الأساسية (موجودة في جميع العقارات):
- ✅ `title` - العنوان
- ✅ `description` - الوصف
- ✅ `property_type` - نوع العقار
- ✅ `listing_type` - نوع العرض (sale/rent)
- ✅ `price` - السعر
- ✅ `area` - المساحة
- ✅ `location` - الموقع
- ✅ `market` - السوق
- ✅ `images` - صورة واحدة على الأقل
- ✅ `is_published` - منشور (true)

#### الحقول المتخصصة (حسب النوع):
- ✅ **للشقق**: building, apartment, floor, bedrooms, bathrooms, elevator
- ✅ **للفلل**: land_area, swimming_pool, maid_room, driver_room
- ✅ **للأراضي**: land_type, street_width, corner, licensed
- ✅ **للتجاري**: shop_type, office_number, cold_storage
- ✅ **وأكثر...**

---

## 🧪 اختبار البيانات

### للتحقق من البيانات المُدخلة:

```sql
-- عرض جميع العقارات المضافة
SELECT 
  property_type,
  title,
  listing_type,
  price,
  area
FROM public.properties
ORDER BY property_type, created_at DESC;

-- إحصائيات حسب النوع
SELECT 
  property_type,
  COUNT(*) as count,
  AVG(price) as avg_price
FROM public.properties
GROUP BY property_type
ORDER BY count DESC;

-- عرض العقارات الأخيرة فقط
SELECT 
  property_type,
  title,
  price,
  created_at
FROM public.properties
WHERE created_at > NOW() - INTERVAL '1 hour'
ORDER BY created_at DESC;
```

---

## 🗑️ حذف البيانات الاختبارية

### إذا أردت حذف جميع البيانات الاختبارية:

```sql
-- ⚠️ تحذير: سيحذف جميع عقارات المستخدم!
-- استبدل user_id بالـ ID الذي استخدمته

DELETE FROM public.properties 
WHERE user_id = 'your-user-id-here'
  AND created_at > NOW() - INTERVAL '1 day';

-- أو حذف عقارات محددة فقط
DELETE FROM public.properties 
WHERE title LIKE '%فخم%' 
   OR title LIKE '%اختبار%';
```

---

## 🎯 استخدام البيانات للاختبار

### اختبار المكونات الجديدة:

#### 1. PropertyCard المحسّن
```typescript
// يمكنك الآن اختبار البطاقة مع 20 عقار مختلف
// كل عقار له حقول مختلفة حسب نوعه
```

#### 2. نظام Pagination
```typescript
// مع 20 عقار، يمكنك اختبار:
- عرض 10 عقارات لكل صفحة (2 صفحة)
- عرض 20 عقار لكل صفحة (صفحة واحدة)
- التنقل بين الصفحات
```

#### 3. Skeleton Loaders
```typescript
// اختبر عرض Skeleton مع قوائم مختلفة الأحجام
```

#### 4. الفلترة والبحث
```typescript
// يمكنك اختبار الفلترة حسب:
- نوع العقار (22 نوع مختلف)
- نوع العرض (بيع/إيجار)
- السعر
- المساحة
- الموقع
```

---

## 📝 تعديل البيانات

### لإضافة المزيد من العقارات:

1. انسخ أحد النماذج من الملف
2. عدّل القيم (العنوان، السعر، المساحة، الخ)
3. أضف INSERT جديد

### مثال - إضافة شقة جديدة:

```sql
INSERT INTO public.properties (
  user_id, title, description, property_type, listing_type, price, area,
  bedrooms, bathrooms, building, apartment, floor, location, market,
  balcony, parking, elevator, furnished, is_published
) VALUES (
  'your-user-id',
  'شقة عادية 2 غرف',
  'شقة بسيطة في موقع جيد',
  'apartment', 'rent', 2500, 120,
  2, 1, 'عمارة 10', 'شقة 5', 'الطابق الأول',
  'الرياض، حي السلام', 'جنوب الرياض',
  false, true, false, 'yes', true
);
```

---

## 💡 نصائح

### 1. للتطوير المحلي:
```
- استخدم user_id واحد لجميع العقارات
- أعط العقارات أسعار ومساحات واقعية
- استخدم مواقع حقيقية من مدينتك
```

### 2. للاختبار:
```
- جرّب سيناريوهات مختلفة
- اختبر الفلترة والبحث
- اختبر Pagination مع أحجام مختلفة
```

### 3. للإنتاج:
```
- احذف البيانات الاختبارية قبل النشر
- استخدم بيانات حقيقية من المستخدمين
```

---

## 🎉 الخلاصة

الآن لديك:
- ✅ 20 عقار اختباري
- ✅ يغطي جميع الأنواع (22 نوع)
- ✅ بيانات واقعية وكاملة
- ✅ جاهز للاستخدام في الاختبار
- ✅ سهل التعديل والإضافة

**استمتع بالاختبار!** 🚀

---

**آخر تحديث**: 20 نوفمبر 2025  
**الإصدار**: 1.0  
**الحالة**: ✅ جاهز
